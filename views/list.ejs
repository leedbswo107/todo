<%- include('header.ejs') %>
<h2>목록페이지 ------</h2>
<ul>
  <%- post.length === 0 ? `<li>등록된 리스트가 없습니다</li>` : post.map((element) => `
  <li>
    <p>${element.title}</p>
    <p>${element.dateOfGoals}</p>
    <button>삭제</button>
    <a href="/detail/${element._id}">상세페이지</a>
  </li>
  `).join('');
  %>
</ul>
<%- include('footer.ejs') %>
